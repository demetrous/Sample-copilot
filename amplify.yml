version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands:
        - echo "No build step for static site"
  artifacts:
    # For a plain static site served from the repository root
    baseDirectory: /
    files:
      - "**/*"
  cache:
    paths: []
